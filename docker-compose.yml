services:
  # Chroma Vector Database
  chroma:
    image: chromadb/chroma:latest
    container_name: research-agent-chroma
    ports:
      - "8000:8000"
    volumes:
      - ./storage/chroma:/chroma/chroma
    environment:
      - CHROMA_SERVER_AUTH_PROVIDER=chromadb.auth.token_authn.TokenAuthenticationServerProvider
      - CHROMA_SERVER_AUTH_CREDENTIALS=${CHROMA_AUTH_TOKEN:-default-token}
      - CHROMA_SERVER_AUTH_TOKEN_TRANSPORT_HEADER=X-Chroma-Token
      - ANONYMIZED_TELEMETRY=False
    networks:
      - agent-network

  # Nginx reverse proxy to handle CORS for Chroma + Swagger UI
  chroma-ui:
    image: nginx:alpine
    container_name: research-agent-chroma-ui
    ports:
      - "3001:80"
    volumes:
      - ./docker/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - chroma
    networks:
      - agent-network
    profiles:
      - dev

networks:
  agent-network:
    driver: bridge

volumes:
  chroma-data: